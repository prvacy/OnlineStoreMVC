@model ICollection<MVCTest.Models.Product.Category>

@{
    ViewData["Title"] = "Product";
    Layout = "_Layout";

}
@*TODO: Delete this file, bot needed*@
<!DOCTYPE html>
<html>
<head>
    <title></title>
    <style>
        body {
            color: ghostwhite;
        }

        input, textarea, select {
            color: black;
        }
    </style>
    <link href="~/css/form.css" rel="stylesheet" />
</head>
<body>
    <form id="form" class="topBefore" method="post" enctype="multipart/form-data">
        <p class="text-up-input">NAME</p>
        <input type="text" name="Name" />

        <p class="text-up-input">PRICE</p>
        <input type="text" name="Price_string" step="0.05" />

        <p class="text-up-input">QUANTITY</p>
        <input type="number" name="Quantity" />

        <p class="text-up-input">DESCRIPTION</p>
        <textarea type="text" placeholder="MESSAGE"></textarea>

        <p class="text-up-input">CATEGORY</p>
        <select name="subCategoryId">
            @foreach (var item in Model)
            {
                <optgroup label="@item.Name">

                    @foreach (var subc in item.SubCategories)
                    {
                        <option value="@subc.Id">@subc.Name</option>
                    }

                </optgroup>
            }
        </select>

        <p class="text-up-input">WEIGHT</p>
        <input type="number" name="Weight_string" step="0.05" />

        <p class="text-up-input">VOLUME</p>
        <input type="number" name="Volume_string" step="0.05" />

        <p class="text-up-input">SERVER IMAGE PATH</p>
        <input type="text" name="ImagePath" />

        <p class="text-up-input">UPLOAD IMAGE</p>
        <input type="file" accept="image/png, image/jpeg" name="file" id="file" />



        <input id="submit" type="submit" value="SUBMIT">
    </form>
    @*<form method="post" enctype="multipart/form-data">
        <table class="table-condensed">
            <tr>
                <td>Name:</td>
                <td><input type="text" name="Name" /> </td>
            </tr>

            <tr>
                <td>Price:</td>
                <td><input type="text" name="Price_string" step="0.05" /></td>
            </tr>

            <tr>
                <td>Quantity:</td>
                <td><input type="number" name="Quantity" /></td>
            </tr>

            <tr>
                <td>Description:</td>
                <td><textarea name="Description"></textarea> </td>
            </tr>

            <tr>
                <td>Weight:</td>
                <td><input type="number" name="Weight_string" step="0.05" /></td>
            </tr>

            <tr>
                <td>Volume:</td>
                <td><input type="number" name="Volume_string" step="0.05" /></td>
            </tr>

            <tr>
                <td>ImagePath:</td>
                <td><input type="text" name="ImagePath" /></td>
            </tr>


            <tr>
                <td>Category:</td>
                <td>
                    <select name="subCategoryId">
                        @foreach (var item in Model)
                        {
                            <optgroup label="@item.Name">

                                @foreach (var subc in item.SubCategories)
                                {
                                    <option value="@subc.Id">@subc.Name</option>
                                }

                            </optgroup>
                        }
                    </select>
                </td>
            </tr>

            <tr>
                <td>Image:</td>
                <td><input type="file" name="file" /></td>
            </tr>

            <tr>
                <td><input type="submit" value="Submit" /> </td>
            </tr>

        </table>
    </form>*@
</body>
</html>

